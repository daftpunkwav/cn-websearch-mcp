node_modules/
dist/
.env
*.log
coverage/
# The JSON config file may contain API keys, so it is ignored by default to make
# committing a secret by accident harder. Use `git add -f` if you keep a keyless,
# shareable config there (see cn-websearch.config.example.json).
cn-websearch.config.json
